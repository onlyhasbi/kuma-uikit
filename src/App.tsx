function App() {
  return <div>This website Under Contruction</div>;
}

export default App;

{
  /* <Flex>
<Sidebar w={225} isCollapsible>
  <Sidebar.Title bg={"red"}>
    <VStack alignItems={"center"} gap={2}>
      <LibraryBig size={65} color="#fff" />
      <Text>Malibs</Text>
    </VStack>
  </Sidebar.Title>
  <Sidebar.Content bg={"blue"} p={15}>
    {(collapsible) => (
      <Accordion
        key={+new Date()}
        title={
          <HStack
            as="button"
            gap={3}
            onClick={iconToggle}
            border={"none"}
            bg={"transparent"}
            cursor={"pointer"}
            alignItems={"center"}
          >
            <File color="white" />
            {collapsible && <Text color={"white"}>File</Text>}
          </HStack>
        }
        container={{ bg: "black" }}
        items={["test"]}
        isCollapsible={collapsible}
      />
    )}
  </Sidebar.Content>
  <Sidebar.Footer bg={"purple"}>Footer</Sidebar.Footer>
</Sidebar>
<Flex style={{ padding: 10 }} alignItems={"start"}>
  <VStack alignItems={"center"} gap={10}>
    <Text icon={<Aperture size={20} />} iconPosition="right">
      Label
    </Text>
    <Input
      placeholder="Test input"
      length={10}
      icon={
        <Icon
          icon={<Eye size={16} />}
          color={defaultTheme.colors.border}
          hover={defaultTheme.colors.primary}
        />
      }
      iconPosition="right"
    />
    <AriaInput
      placeholder="this is aria"
      label="Label dummy"
      helperMessage="lorem ipsum helper"
      borderColor="orange"
      {...form.register("test")}
    />
  </VStack>
</Flex>
</Flex> */
}
